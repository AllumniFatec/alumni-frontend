export class MockPostsApi {
  static async getUser(userId: string) {
    try {
    } catch (error) {
      console.error(error);
    }
  }

  static async updateUser(userId: string, data: any) {
    try {
    } catch (error) {
      console.error(error);
    }
  }
}
